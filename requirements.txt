# ==============================================================================
# Wetterprojekt — requirements.txt
# Stand: nach Refactor-Korrekturen
# Zielplattform: Raspberry Pi 4/5 (64-bit) mit Python 3.10+
# ==============================================================================
#
# Installation:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install --upgrade pip wheel setuptools
#   pip install -r requirements.txt
#
# Bei Problemen mit tensorflow auf Raspberry Pi:
#   pip install --extra-index-url https://www.piwheels.org/simple tensorflow
#
# ==============================================================================

# --- Numerik & Datenframes ---
numpy>=1.24,<2.0
pandas>=2.0
pyarrow>=15.0

# --- Machine Learning (klassisch) ---
scikit-learn>=1.3
lightgbm>=4.0
joblib>=1.3

# --- Deep Learning ---
# Achtung: tensorflow ist groß (~500 MB). Auf Pi 5 problemlos, auf Pi 4
# mit 4 GB RAM beim ConvLSTM-Training auf Swap achten.
tensorflow>=2.13,<2.17

# --- Scheduling ---
apscheduler>=3.10

# --- Computer Vision ---
# headless-Variante: kein X11/Qt nötig (perfekt für headless Pi)
opencv-python-headless>=4.8

# --- Geo / Geometrie ---
shapely>=2.0
geopy>=2.3
pyproj>=3.6

# --- Filter / Tracking ---
filterpy>=1.4

# --- Web / Adminpanel ---
flask>=3.0
werkzeug>=3.0
jinja2>=3.1
PyJWT>=2.8
bcrypt>=4.0

# --- Visualisierung (für /progress-Plots und evaluation.py) ---
matplotlib>=3.7
Pillow>=10.0

# --- Karte (Server-seitige Folium-Helfer; Frontend nutzt Leaflet via CDN) ---
# der Pipeline können es aber noch importieren — sicherheitshalber drin lassen.

# --- HTTP / Datenquellen ---
requests>=2.31
urllib3>=2.0
beautifulsoup4>=4.12
lxml>=4.9

# --- KML/KMZ-Export ---
simplekml>=1.3

# --- GeoTIFF / WMS (für cloud_height_from_eumetview) ---
# rasterio braucht libgdal-dev als System-Paket (in install.sh enthalten).
rasterio>=1.3

# --- Konfiguration / Secrets (für die in der Anleitung empfohlene .env-Auslagerung) ---
python-dotenv>=1.0

# --- Anthropic API (KI-Analyse-Pipeline, optional) ---
anthropic>=0.30

# --- Zeitzonen (auf Python 3.9+ ist zoneinfo stdlib, tzdata braucht es trotzdem) ---
tzdata>=2024.1

# ==============================================================================
# Entwicklungs-Tools (optional, nur lokal nötig — auskommentieren für Prod)
# ==============================================================================
# pytest>=8.0
# black>=24.0
# ruff>=0.4
# ipython>=8.0
pysteps>=1.8.0
scipy>=1.11.0
